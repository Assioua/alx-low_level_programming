#include <stdio.h>

/**
 * main - print all alphabets
 * Return: return success
*/

int main(void)
{
	char ch;

	for (ch = 'a' ; ch <= 'z' ; ch++)
	{
		putchar(ch);
	}
		putchar('\n');
	return (0);
}
